import 'package:flutter/material.dart';

class FormAddEducation extends StatelessWidget {
  const FormAddEducation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
