import 'package:flutter/material.dart';

class SubFeedPage extends StatelessWidget {
  const SubFeedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
